[project]
name = "agentic-legibility"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "boto3>=1.43.40",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
